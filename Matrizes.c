#include <stdio.h>

main()
{
    int n, m, i, ii;

    scanf("%i %i", &n, &m);

    int celula[n][m], t=0, x=0, a=0, b=0, c=0, y=0

     for(i=0; i<2; i++)
    {
        for(ii=0; ii<4; ii++)
        {
            scanf()
        }
    }
}
